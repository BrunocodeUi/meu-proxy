daemon
log /dev/stdout
maxconn 1000

# DNS
nserver 1.1.1.1
nserver 8.8.8.8
nscache 65536

# Autenticação com variáveis de ambiente
auth strong
users $(PROXY_USER):CL:$(PROXY_PASS)

allow $(PROXY_USER)

# SOCKS5 (melhor para Evolution)
socks -p1080

# HTTP Proxy
proxy -p3128

flush